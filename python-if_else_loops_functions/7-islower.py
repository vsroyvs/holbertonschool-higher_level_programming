#!/usr/bin/python
def islower(c):
    if (ord(c) > 96 and ord(c) < 123):
        #print("{} is lower".format(c))
        return (1)
    else:
        #print("{} is upper".format(c))
        return (0)
